'use client';

import CarDetailsDrawer, {
  type CarDetailsDrawerProps,
} from '../CarDetailsDrawer';

const MobileCarDetailsDrawer = (props: CarDetailsDrawerProps) => (
  <CarDetailsDrawer {...props} />
);

export default MobileCarDetailsDrawer;
